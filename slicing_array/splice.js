const arr1 = [1, 2, 3, 4, 5, 6];

// (start, deleteCount, replacement)
// this method slices an array in place
// and returns what is sliced.
const spliced = arr1.splice(2);
console.log(arr1);
console.log(spliced);
